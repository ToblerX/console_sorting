class Algo(object):
    def __init__(self, arr: list, count_time: bool) -> None:
        self.arr = arr
        self.count_time = count_time
    def run(self, arr: list, count_time: bool) -> list: #not list but Tuple, because of static order of elements of different type
        """Sort the array using algorithm X"""
        pass
